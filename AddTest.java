package edu.depaul.se433.week3;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class AddTest {

	@Test
	void testAdd() {
		//var adder = new Add();
		//var result = adder.execute(1.0, 2.0);
		//assertEquals(3.0, result, 1e-2);
	}
	
	@Test
	@DisplayName("A value plus it's negation should equal zero")
	void testAddNegative() {
		//var adder = new Add();
		//var result = adder.execute(1.0, -1.0);
		//assertEquals(0.0, result, 1e-2);
	}

}
